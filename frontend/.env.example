# Backend API URL (required for production when frontend is on Vercel)
# Local dev: leave unset to use http://localhost:4000
# Production: set to your Render backend URL (no trailing slash)
REACT_APP_API_URL=https://juridikatest.onrender.com
